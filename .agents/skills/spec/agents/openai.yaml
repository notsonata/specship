interface:
  display_name: "Spec"
  short_description: "Create, update, and review implementation plans"
  default_prompt: "Use $spec to investigate the repository and create, update, or review a self-contained implementation plan without implementing it."

policy:
  allow_implicit_invocation: false
