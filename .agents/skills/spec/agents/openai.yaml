interface:
  display_name: "Spec"
  short_description: "Create and review sealed implementation plans"
  default_prompt: "Use $spec to create, refine, review, or explicitly finalize a versioned repository plan without implementing it."

policy:
  allow_implicit_invocation: false
