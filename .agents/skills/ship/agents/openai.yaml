interface:
  display_name: "Ship"
  short_description: "Implement one complete sealed plan"
  default_prompt: "Use $ship to implement or resume this complete sealed plan, validate it, and record revision-pinned evidence: docs/plans/plan-name."

policy:
  allow_implicit_invocation: false
