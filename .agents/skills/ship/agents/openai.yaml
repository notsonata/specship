interface:
  display_name: "Ship"
  short_description: "Implement one complete repository plan"
  default_prompt: "Use $ship to implement or resume this complete plan, validate it, and record evidence in RESULTS.md: docs/plans/plan-name."

policy:
  allow_implicit_invocation: false
