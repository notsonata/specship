interface:
  display_name: "Ship"
  short_description: "Implement a complete plan with discipline"
  default_prompt: "Use $ship to implement this complete plan, validate it, and record evidence: docs/plans/plan-name."

policy:
  allow_implicit_invocation: false
